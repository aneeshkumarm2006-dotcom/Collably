/** Business "Messages" tab — chat threads with the creators they've approved. */
import { ConversationListScreen } from '@/components/chat';

export default function BusinessMessagesScreen() {
  return <ConversationListScreen group="(business)" />;
}
