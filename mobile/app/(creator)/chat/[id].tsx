/** Creator chat thread (pushed over the tabs). Role-agnostic shared body. */
import { ChatThreadScreen } from '@/components/chat';

export default function CreatorChatThread() {
  return <ChatThreadScreen />;
}
