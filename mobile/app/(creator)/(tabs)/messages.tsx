/** Creator "Messages" tab — chat threads with the businesses they've collab'd with. */
import { ConversationListScreen } from '@/components/chat';

export default function CreatorMessagesScreen() {
  return <ConversationListScreen group="(creator)" />;
}
