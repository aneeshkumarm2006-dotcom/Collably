/**
 * Barrel for the onboarding building blocks (PRD §7.2, Phase 11). The two role
 * onboarding screens import the shell from here; step bodies live inline in each
 * screen since they don't need to be shared.
 */
export { OnboardingShell, type OnboardingShellProps } from './OnboardingShell';
