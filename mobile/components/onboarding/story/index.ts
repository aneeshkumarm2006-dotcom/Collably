/**
 * Barrel for the immersive "story panel" onboarding (Direction C): the full-screen
 * shell plus the kit of panels, tiles, and progress chrome that render over it.
 */
export { StoryOnboarding, type StoryOnboardingProps } from './StoryOnboarding';
export {
  StoryPanel,
  StoryHeadline,
  ChoiceTile,
  NextPill,
  SkipLink,
  StoryInput,
  StoryAutocomplete,
  StoryProgress,
  StoryBackButton,
  WelcomeDeck,
  NICHE_VISUAL,
  CONTENT_VISUAL,
  optionVisual,
  useTileWidth,
  type Grad,
} from './StoryKit';
